apiVersion: v1
kind: Service
metadata:
  labels:
    app: defguard
    service: db
  name: db
spec:
  ports:
    - name: "db"
      port: 5432
      targetPort: 5432
  selector:
    app: defguard
    service: db
